-- Insert sample money buckets (only run after user registration)
-- This is just an example - in real app, users will create their own buckets

-- Sample data for demonstration (replace user_id with actual user ID)
-- INSERT INTO money_buckets (name, balance, user_id) VALUES
-- ('Wallet', 500.00, 'user-uuid-here'),
-- ('Bank Account', 2500.00, 'user-uuid-here'),
-- ('PayPal', 150.00, 'user-uuid-here');

-- Sample goals
-- INSERT INTO goals (title, target_amount, current_amount, user_id) VALUES
-- ('Emergency Fund', 5000.00, 1200.00, 'user-uuid-here'),
-- ('New Laptop', 1500.00, 300.00, 'user-uuid-here'),
-- ('Vacation', 3000.00, 800.00, 'user-uuid-here');

-- Note: Replace 'user-uuid-here' with actual user UUIDs from auth.users table
-- These are just examples - in production, users will create their own data
